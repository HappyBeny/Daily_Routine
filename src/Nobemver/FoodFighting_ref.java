package Nobemver;

import java.util.*;

public class FoodFighting_ref {
/*    public String solution(int[] food) {
        String answer = "";
        List<Integer> checker = new ArrayList<Integer>();
        StringBuilder sb = new StringBuilder("");
        int index1 = 1;

        for (int i = 1; i < food.length; i++) {
            food[i] -= (food[i] % 2);
            index1 += food[i];
            food[i] /= 2;
        }

        for (int i = 1; i < food.length; i++) {
            checker.add(i);
        }

        checker[(index1 - 1) / 2] = "0";


        for (String s : checker) {
            sb.append(s);
        }
        answer = sb.toString();
        return answer;
    }*/
}